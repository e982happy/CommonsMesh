{
  "name": "commonsmesh",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first community mutual aid and resource matching infrastructure",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "demo": "node --enable-source-maps dist/examples/demo-node.js",
    "check": "npm run build",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
